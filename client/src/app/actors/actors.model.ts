export interface ActorCreationDTO {
  name: string;
  dateOfBirth: Date;
  picture: string;
  bio?: string;
}

export interface ActorDTO {
  name: string;
  dateOfBirth: Date;
  picture: string;
  bio?: string;
}
